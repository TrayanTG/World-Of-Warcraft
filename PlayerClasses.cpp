#include "PlayerClasses.h"

Warrior::Warrior(): Player()
{

}
/*
Damage Warrior::dealDamage(int slot)
{
	if (eqAbilities[slot]->getID < 0)return { 0,0 };
	if (eqAbilities[slot]->getResReq() < res.Curr)return{ 0,0 };
	return Player::dealDamage(slot);
}*/